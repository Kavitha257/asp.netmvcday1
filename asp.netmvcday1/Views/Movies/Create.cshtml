@model asp.netmvcday1.Models.Movie
@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Create</h2>
<div class="container">
    @using (Html.BeginForm())
    {
        <div class="form-group">
            @Html.LabelFor(c => c.Name)
            @Html.TextBoxFor(c => c.Name, new { @class = "form-control", autocomplete = "off" })
            @Html.ValidationMessageFor(c => c.Name)
        </div>
        <div class="form-group">
            @Html.LabelFor(c => c.ReleaseDate)
            @Html.TextBoxFor(c => c.ReleaseDate, new { @class = "form-control", type = "date" })
            @Html.ValidationMessageFor(c => c.ReleaseDate)
        </div>
        <div class="form-group">
            @Html.LabelFor(c => c.DateAdded)
            @Html.TextBoxFor(c => c.DateAdded, new { @class = "form-control", type = "date" })
            @Html.ValidationMessageFor(c => c.DateAdded)
        </div>
        <div class="form-group">
            @Html.LabelFor(c => c.AvailableStock)
            @Html.TextBoxFor(c => c.AvailableStock, new { @class = "form-control" })
            @Html.ValidationMessageFor(c => c.AvailableStock)

        </div>
        <div class="form-group">
            @Html.LabelFor(c => c.Genres)
            @*@Html.DropDownList("GenreId", null, new { @class = "form-control", autocomplete = "off" })*@
            @Html.DropDownListFor(c => c.GenreId, ViewBag.GenreId as SelectList, new { @class = "form-control" })
            @Html.ValidationMessageFor(c => c.GenreId)

        </div>
        <div>
            <input type="submit" name="cmdsubmit" value="Submit" />
        </div>
    }
</div>
